# textbook-ai-mcq — Phase 0 dependencies
# Installed into the existing conda env `bio-ai` (Python 3.11.15).
# Versions pinned to what is present in that environment.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
SQLAlchemy==2.0.35
PyMuPDF==1.28.2
python-docx==1.1.0
httpx==0.27.2
python-dotenv==1.0.1
pytest==9.1.1

# --- Phase 2: hybrid retrieval (BM25 + dense) ---
jieba==0.42.1            # Chinese tokenizer, shared by indexing and retrieval
rank-bm25==0.2.2         # BM25Okapi (numpy-only); vectors go through httpx + numpy

# --- Reserved for later phases (not installed yet) ---
# psycopg2-binary   # PostgreSQL driver (vector DB phase)
# pgvector          # pgvector SQLAlchemy support (vector DB phase)
