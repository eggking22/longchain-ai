# Phase 0：textbook-ai-mcq 项目初始化计划

## 现状（已探查）
- 工作目录 `D:\openclaw\workspace-agent-2\bio-ai-system2.0\langchain-ai` 为空，git 仓库根在 `workspace-agent-2`（main 分支）
- `bio-ai` conda 环境：`C:\Users\Lenovo\anaconda3\envs\bio-ai`，Python 3.11.15
- 环境中已装：fastapi 0.115.0、uvicorn 0.30.6、pydantic 2.9.2、pydantic-settings 2.5.2、SQLAlchemy 2.0.35、python-docx 1.1.0、httpx 0.27.2、python-dotenv、anyio
- 缺失需补装：pytest、PyMuPDF（psycopg2-binary / pgvector 暂不安装，仅保留接口）

## 实施步骤

1. **创建目录结构**（在 `langchain-ai/textbook-ai-mcq/` 下）：
   ```
   textbook-ai-mcq/
   ├── app/  (__init__.py, main.py, core/, db/, models/, schemas/, routers/, services/, llm/, utils/)
   ├── data/  uploads/  prompts/  scripts/  tests/
   ├── requirements.txt  .env.example  .gitignore  README.md
   ```

2. **requirements.txt**：固定已装版本（fastapi==0.115.0、uvicorn==0.30.6、pydantic==2.9.2、pydantic-settings==2.5.2、SQLAlchemy==2.0.35、PyMuPDF、python-docx==1.1.0、httpx==0.27.2、python-dotenv、pytest）；`psycopg2-binary`、`pgvector` 以注释形式保留并标注"后续 Phase 启用"

3. **补装依赖**：直接用 `C:/Users/Lenovo/anaconda3/envs/bio-ai/python.exe -m pip install pytest PyMuPDF`（不 activate、不新建任何虚拟环境）

4. **最小代码**：
   - `app/core/config.py`：pydantic-settings 读取 .env（APP_NAME、DATABASE_URL 占位等）
   - `app/routers/health.py`：`GET /api/v1/health` → `{"status": "ok"}`
   - `app/main.py`：创建 FastAPI 实例（title/description），挂载 health 路由
   - `db/ models/ schemas/ services/ llm/ utils/` 仅放 `__init__.py` 占位（保留接口，不实现 RAG/LLM/parser）
   - `.gitkeep` 保持 data/uploads/prompts/scripts 空目录

5. **tests/test_health.py**：用 fastapi.testclient（httpx 已装）断言 health 接口返回 200 与 `{"status": "ok"}`；附 `pytest.ini`（或 pyproject 最小配置）保证从项目根可直接 `pytest`

6. **配置与文档**：
   - `.env.example`：DATABASE_URL、LLM_API_KEY/LLM_BASE_URL 等占位
   - `.gitignore`：Python 标准 + `.env` + `data/`、`uploads/` 内容（保留目录本身）
   - `README.md`：项目目标、目录说明、启动/测试命令、路线图（Phase 0 已完成，后续 parser/embedding/RAG/LLM/出题/Reviewer）

7. **验收**：
   - 用 bio-ai 的 python 运行 `pytest`（须全绿）
   - 后台启动 `uvicorn app.main:app --reload`，curl `127.0.0.1:8000/api/v1/health`（应返回 `{"status":"ok"}`）与 `/docs`（应返回 200 HTML），验证后停止服务

8. **收尾（按 AGENTS.md）**：更新 workspace 根的 `Decision.md`（记录：使用 bio-ai env、Python 3.11.15、版本锁定、pgvector 暂缓）与 `Hand-off.md`（进度/下一步）；`git status` 检查后，仅 add `langchain-ai/`、`Decision.md`、`Hand-off.md` 提交（不触碰 `bio-ai-system/`），保持 git history 干净

## 明确不做
PDF parser、embedding、向量库、RAG、LLM 调用、出题逻辑（后续 Phase）

## 验收报告将包含
目录结构树、依赖清单（含版本）、pytest 输出结果、uvicorn 启动状态、/docs 可访问性确认